// Write a program that can print out the numeric value of the newline character (\n)
#include <stdio.h>
int main()
{
    char newl = '\n';
    printf("ANSI code of Newline character is %d\n", newl);
    return 0;
}